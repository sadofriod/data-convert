// import { isObject } from "lodash"

export const getValue:Common.GetValue = (obj,val) =>{
  /**
   * TODO: 
   * 1. Get previous node calculation result
   * 2. Append argument for caching current node calculation result
  */
  const {previous,operation} = val;
  //
  
}